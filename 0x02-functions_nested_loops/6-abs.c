#include "main.h"

/**
 * _abs - Entry point
 * Compute the absolute value of an integer
 *
 * @n: the integer to check
 *
 *Return: the absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

}
